<%= link_to("Back to pictures", "/pictures")%> | <%= link_to("Edit", "/pictures/#{@picture.id}/edit") %> | <%= link_to("Delete", "/pictures/#{@picture.id}", method: :delete, data: {confirm: "Are you sure you want to delete this pictures?"})%>

<p><%= @picture.title%>, <%= @picture.artist%> </p>
<%= image_tag(@picture.url)%>
